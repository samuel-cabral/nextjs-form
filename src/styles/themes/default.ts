import { createTheme } from '@mui/material'

export const theme = createTheme({
  palette: {
    mode: 'dark',
    primary: {
      main: '#663399',
    },
    background: {
      default: '#121212',
      paper: '#1E1E1E',
    },
  },
})
